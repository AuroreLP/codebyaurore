{% extends 'admin_base.html.twig' %}
<h1>Messages reçus</h1>

<table>
    <thead>
        <tr>
            <th>Nom</th>
            <th>Email</th>
            <th>Message</th>
        </tr>
    </thead>
    <tbody>
        {% for message in messages %}
            <tr>
                <td>{{ message.name }}</td>
                <td>{{ message.email }}</td>
                <td>{{ message.message }}</td>
            </tr>
        {% endfor %}
    </tbody>
</table>

{% endblock %}
