{% extends 'admin_base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
<div class="form">
  <h1 class="tag-form__title">{{ title }}</h1>

  {{ form_start(form, {'attr': {'class': 'tag-form__form'}}) }}
    <div class="tag-form__group">
      {{ form_label(form.name, null, {'label_attr': {'class': 'tag-form__label'}}) }}
      {{ form_widget(form.name, {'attr': {'class': 'tag-form__input'}}) }}
      {{ form_errors(form.name) }}
    </div>
    <div class="tag-form__group">
      {{ form_label(form.slug, null, {'label_attr': {'class': 'tag-form__label'}}) }}
      {{ form_widget(form.slug, {'attr': {'class': 'tag-form__input'}}) }}
      {{ form_errors(form.slug) }}
    </div>

    <div class="tag-form__actions">
      <button type="submit" class="button--dark-green">Enregistrer</button>
      <a href="{{ path('admin.tags.index') }}" class="button--light-green">Retour</a>
    </div>
  {{ form_end(form) }}
</div>
{% endblock %}
