services:
  www:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: codebyaurore_php
    restart: always
    ports:
      - 8080:8080
    volumes:
      - ./:/var/www/html
      - ./docker/php/vhosts:/etc/apache2/sites-enabled
    env_file:
      - .env.local
    depends_on:
      - mongodb
      - mysql
    networks:
      - codebyaurore
    # user: "33:33" à décommenter en production

  mysql:
    image: mysql
    container_name: codebyaurore_mysql
    restart: always
    environment:
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - codebyaurore
    env_file:
      - .env.local

  mongodb:
    image: mongo:6.0
    container_name: codebyaurore_mongodb
    volumes:
      - mongo_data:/data/db
    networks:
      - codebyaurore

networks:
  codebyaurore:
volumes:
  mongo_data:
  mysql_data:
